import classNames from "classnames";
import "../styles/FriendListItem.scss";

// TODO: FriendListItem needs to be implemented

const FriendListItem = (props) => {
  const {} = props;
  const friendItemClass = classNames();
  return;
};

export default FriendListItem;
